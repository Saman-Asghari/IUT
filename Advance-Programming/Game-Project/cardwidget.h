#include <QWidget>
#include <QPixmap>
#include <QVBoxLayout>
#include <QLabel>

class CardWidget : public QWidget
{
public:
    CardWidget(const QString& imagePath, QWidget *parent = nullptr);
private:
};
